<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Event</h1>
  		<label>Title</label>
  		<%= @event.title %>
  		<label>Content</label>
  		<%= @event.content %>
  		<%= link_to 'Back', group_path(@group), class: "back" %>
  </div>
</div>