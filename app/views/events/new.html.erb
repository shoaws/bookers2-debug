<div class='container'>

  <%= render 'layouts/errors', obj: @event %>

  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>
  		<%= render 'form', group: @group, event: @event  %>
  		<%= link_to 'Back', group_path(@group), class: "back" %>
  </div>
</div>